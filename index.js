/**
 * @format
 */

import {AppRegistry} from 'react-native';
import {name as appName} from './app.json';
import { CustomTabApp } from './src/CustomTabApp';

AppRegistry.registerComponent(appName, () => CustomTabApp);
