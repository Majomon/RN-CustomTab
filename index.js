/**
 * @format
 */

import {AppRegistry} from 'react-native';
import {name as appName} from './app.json';
import { TabApp } from './src/TabApp';

AppRegistry.registerComponent(appName, () => TabApp);
